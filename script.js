const canv = document.getElementById("_canvas")
let ctx = canv.getContext("2d")

canv.innerWidth = window.innerWidth
canv.innerHeight = window.innerHeigh

//ctx.fillStyle = "#ff0000"
//ctx.fillRect(0, 0, 100, 100)

class rechteck {
    erstellen() {
        console.log("test")
    }
}

let x = new rechteck
x.erstellen()
